server:
  port: 8081

spring:
  application:
    name: userService

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/custom?useSSL=false&characterEncoding=UTF-8
    username: root
    password: root

  cloud:
    nacos:
      server-addr: localhost:8848


mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true

