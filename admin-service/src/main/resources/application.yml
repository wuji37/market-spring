server:
  port: 8082

spring:
  application:
    name: adminService

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/admin?useSSL=false&characterEncoding=UTF-8
    username: root
    password: root


  cloud:
    nacos:
      server-addr: localhost:8848

mybatis:
  type-aliases-package: cn.itcast.admin.pojo
  configuration:
    map-underscore-to-camel-case: true
